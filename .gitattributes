# 二进制文件：显式声明，避免被 Git 误判为文本而做行尾转换
# （例如部分 PDF 生成器的输出会被误判；一旦发生行尾转换可能损坏文件）
*.pdf binary
*.jpg binary
*.jpeg binary
*.png binary
*.zip binary
*.epro2 binary

# 交互式 BOM（生成型 HTML 导出，约 12.6 MB）：不做文本 diff、不计入语言统计
Hardware/Manufacturing/BOM/InteractiveBOM_*.html -diff linguist-vendored linguist-generated

# README 开场实拍图（板卡合照）
Images/合照.jpg binary
